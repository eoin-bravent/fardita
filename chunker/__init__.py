"""FARDITA — clean-room package.

Rebuilt from the proven pipeline/ modules (parser eras + SCD-2 merge), reorganized
into a single package with one CLI, one ingest layer, and pluggable edition sources.
pipeline/ stays as the working reference until chunker/ passes every gate, then retires.
"""
